# pyproject.toml

[tool.isort]
  profile = "black"
  multi_line_output = 3

[tool.pytest.ini_options]
  asyncio_mode = "auto"

[mypy]
  follow_imports = "skip"
  exclude = "ramses_rf"

[mypy-ramses_rf]
  ignore_errors = true

[mypy-tests]
  ignore_errors = true